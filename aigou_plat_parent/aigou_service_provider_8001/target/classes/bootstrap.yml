spring:
  cloud:
    config:
      uri: http://127.0.0.1:1299 #配置服务器
      label: master #分支
      name: service_provider_application #github上面名称
      profile: dev
#      profile: ${spring.actives.profile} # 动态获取运行时的参数
